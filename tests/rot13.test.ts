import { describe, it, expect } from "vitest"; import { rot13 } from "../src/index"; describe("rot13", () => { it("transforms lowercase correctly", () => { expect(rot13("hello")).toBe("uryyb"); }); it("transforms uppercase correctly", () => { expect(rot13("HELLO")).toBe("URYYB"); }); it("is symmetric", () => { const original = "breuer.marcel@outlook.com"; expect(rot13(rot13(original))).toBe(original); }); it("preserves non-letters", () => { expect(rot13("123-_=!")).toBe("123-_=!"); }); });